<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Ennerdalians FC</title>
  <link rel="stylesheet" href="style.css" />
</head>
<body>
  <!-- Header -->
  <header>
    <div class="logo">
      <img src="Ennerdalians_FC.png" alt="Ennerdalians FC Logo" />
      <h1>Ennerdalians FC</h1>
    </div>
    <nav>
      <a href="#fixtures">Fixtures</a>
      <a href="#results">Results</a>
      <a href="#news">News</a>
      <a href="#contact">Contact</a>
    </nav>
  </header>

  <!-- Hero Section -->
  <section class="hero">
    <h2>Welcome to Ennerdalians FC</h2>
    <p>Striving for greatness on and off the pitch ⚽</p>
  </section>

  <!-- Fixtures -->
  <section id="fixtures">
    <h2>Upcoming Fixtures</h2>
    <ul id="fixtures-list"></ul>
  </section>

  <!-- Results -->
  <section id="results">
    <h2>Latest Results</h2>
    <ul id="results-list"></ul>
  </section>

  <!-- News -->
  <section id="news">
    <h2>Club News</h2>
    <div id="news-container"></div>
  </section>

  <!-- Contact -->
  <section id="contact">
    <h2>Contact Us</h2>
    <p>Email: <a href="mailto:aubreymalapile@yahoo.com">aubreymalapile@yahoo.com</a></p>
    <p>Phone: 083 415 8217</p>
  </section>

  <footer>
    <p>&copy; 2025 Ennerdalians FC. All Rights Reserved.</p>
  </footer>

  <script src="script.js"></script>
</body>
</html>
/* Reset */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: Arial, sans-serif;
  line-height: 1.6;
  background: #f8f8f8;
  color: #333;
}

/* Header */
header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: #004080;
  color: #fff;
  padding: 1rem 2rem;
}

header .logo {
  display: flex;
  align-items: center;
}

header .logo img {
  height: 50px;
  margin-right: 10px;
}

header nav a {
  color: #fff;
  margin-left: 15px;
  text-decoration: none;
  font-weight: bold;
}

header nav a:hover {
  text-decoration: underline;
}

/* Hero */
.hero {
  text-align: center;
  background: url("soccer-bg.jpg") center/cover no-repeat;
  color: white;
  padding: 100px 20px;
}

.hero h2 {
  font-size: 2.5rem;
}

.hero p {
  font-size: 1.2rem;
  margin-top: 10px;
}

/* Sections */
section {
  padding: 40px 20px;
  max-width: 1000px;
  margin: auto;
}

section h2 {
  color: #004080;
  margin-bottom: 20px;
  text-align: center;
}

ul {
  list-style: none;
}

ul li {
  background: #fff;
  padding: 15px;
  margin: 10px 0;
  border-radius: 8px;
  box-shadow: 0 2px 5px rgba(0,0,0,0.1);
}

/* Footer */
footer {
  text-align: center;
  background: #004080;
  color: #fff;
  padding: 10px;
  margin-top: 30px;
}
// Dummy Data (replace with live updates later)
const fixtures = [
  { opponent: "Protea United", date: "2025-09-01", venue: "Home" },
  { opponent: "Soweto Stars", date: "2025-09-08", venue: "Away" },
];

const results = [
  { opponent: "Orlando Juniors", score: "3-1", date: "2025-08-20" },
  { opponent: "Dobsonville FC", score: "1-2", date: "2025-08-15" },
];

const news = [
  { title: "Big Win Against Orlando Juniors!", body: "Ennerdalians secured a 3-1 victory in a thrilling match." },
  { title: "New Training Kit Arrives", body: "Our players will now train in brand new kits sponsored by local partners." }
];

// Render Fixtures
const fixturesList = document.getElementById("fixtures-list");
fixtures.forEach(f => {
  const li = document.createElement("li");
  li.textContent = `${f.date} - vs ${f.opponent} (${f.venue})`;
  fixturesList.appendChild(li);
});

// Render Results
const resultsList = document.getElementById("results-list");
results.forEach(r => {
  const li = document.createElement("li");
  li.textContent = `${r.date} - vs ${r.opponent} : ${r.score}`;
  resultsList.appendChild(li);
});

// Render News
const newsContainer = document.getElementById("news-container");
news.forEach(n => {
  const div = document.createElement("div");
  div.classList.add("news-item");
  div.innerHTML = `<h3>${n.title}</h3><p>${n.body}</p>`;
  newsContainer.appendChild(div);
});
